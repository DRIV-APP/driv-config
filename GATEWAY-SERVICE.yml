server:
 port: 9191

hystrix:
  command:
    fallbackcmd:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 4000

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream            

# spring:
#   cloud:
#     gateway:
#       globalcors:
#         add-to-simple-url-handler-mapping: true
#         cors-configurations:
#           '[/**]':
#             allowedOrigins: "https://docs.spring.io"
#             allowedMethods:
#             - GET

# spring:
#   cloud:
#     gateway:
#       routes:
#         - id: ADDRESS-SERVICE
#           uri: lb://ADDRESS-SERVICE
#           predicates:
#             - Path=/api-address/**
#           filters:
#             - name: CircuitBreaker
#               agrs:
#                 name: ADDRESS-SERVICE
#                 fallbackuri: forward:/addressServiceFallBack
#         - id: ITINERARY-CATEGORY-SERVICE
#           uri: lb://ITINERARY-CATEGORY-SERVICE
#           predicates:
#             - Path=/api/**
#           filters:
#             - name: CircuitBreaker
#               agrs:
#                 name: ITINERARY-CATEGORY-SERVICE
#                 fallbackuri: forward:/itineraryCategoryServiceFallBack
#         - id: PASSENGER-SERVICE
#           uri: lb://PASSENGER-SERVICE
#           predicates:
#             - Path=/api/**
#           filters:
#             - name: CircuitBreaker
#               agrs:
#                 name: PASSENGER-SERVICE
#                 fallbackuri: forward:/passengerServiceFallBack


